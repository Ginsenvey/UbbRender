<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CodeDisplay.CodeBlock"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodeDisplay"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Control"
    mc:Ignorable="d">

    <Grid BorderBrush="{Binding BorderBrush,ElementName=Control}"
          BorderThickness="{Binding BorderThickness,ElementName=Control}"
          Background="{Binding Background,ElementName=Control}"
          Padding="{Binding Padding,ElementName=Control}"
          CornerRadius="{Binding CornerRadius,ElementName=Control}"
          Margin="{Binding Margin,ElementName=Control}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" HorizontalAlignment="Left" Orientation="Vertical" Spacing="2">
            <TextBlock x:Name="LanguageTag" Text="C#" FontSize="12" Margin="3,0,3,0"></TextBlock>
            <Border Background="{ThemeResource AccentAAFillColorDefaultBrush}" Height="3" CornerRadius="2"></Border>
        </StackPanel>
        <Button x:Name="CopyButton" Grid.Row="0" HorizontalAlignment="Right" Width="32" Height="32" Padding="0" Margin="0,0,8,0">
            <FontIcon Glyph="&#xE8C8;" FontSize="16"></FontIcon>
        </Button>
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <RichTextBlock x:Name="Viewer" FontFamily="Consolas" IsTextSelectionEnabled="True" FontSize="{Binding FontSize,ElementName=Control}"></RichTextBlock>
        </ScrollViewer>
    </Grid>
</UserControl>
